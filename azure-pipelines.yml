stages:
- stage: Build
  jobs:
  - job: BuildJob
    pool:
      vmImage: 'ubuntu-latest'
    steps:
    - script: echo "Building the application..."
      displayName: 'Build Stage'

# Эта стадия будет скипнута
- stage: Deploy
  dependsOn: []
  condition: false    # <-- Стадия пропускается
  jobs:
  - job: DeployJob
    pool:
      vmImage: 'ubuntu-latest'
    steps:
    - script: echo "Deploying the application..."
      displayName: 'Deploy Stage'

- stage: Deploy1
  dependsOn: []
  jobs:
  - job: DeployJob
    pool:
      vmImage: 'ubuntu-latest'
    steps:
    - script: echo "Deploying the application..."
      displayName: 'Deploy Stage'

# Эта стадия тоже будет скипнута
- stage: Deploy2
  dependsOn: []
  condition: false    # <-- Стадия пропускается
  jobs:
  - job: DeployJob
    pool:
      vmImage: 'ubuntu-latest'
    steps:
    - script: echo "Deploying the application..."
      displayName: 'Deploy Stage'
